<div class="container">
  <div class="row">

    <% if @label.errors.any? %>
      <% @label.errors.full_messages.each do |message| %>
        <%= message %>
      <% end %>
    <% end %>

    <div class="col-xs-4">
      <h3>レーベル登録</h3>
      <%= form_for @label, :url => {:action => :create} do |f| %>
        <%= f.label :レーベル名  %><br>
        <%= f.text_field :name %><br>
        <%= f.submit class:"btn btn-success"%>
      <% end %>
    </div>
    <div class="col-xs-8">
      <h3>レーベル一覧</h3>
      <table class="table">
        <tbody>
          <% @labels.each do |label| %>
            <tr>
              <td>レーベル名</td>
              <td><%= label.name %></td>
              <td><%= link_to "編集", edit_administrator_label_path(label), class:"btn btn-primary" %></td>
              <td><%= link_to "削除", administrator_label_path(label), method: :delete, class:"btn btn-danger"  %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
