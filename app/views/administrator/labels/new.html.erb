<h1>Admin::Labels#new</h1>
<% if @label.errors.any? %>
  <% @label.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>


<%= form_for @label, :url => {:action => :create} do |f| %>
<%= f.label :Name %><br>
<%= f.text_field :name %><br>
<%= f.submit %>
<% end %>
